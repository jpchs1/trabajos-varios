customtkinter>=5.2.2
pandas>=2.2.0
openpyxl>=3.1.2
pyinstaller>=6.0.0

# Búsqueda automática de productos específicos (app/auto_fetch.py).
# Tras instalar, ejecutar una vez: playwright install chromium
# (el programa también lo descarga solo la primera vez que se usa).
playwright>=1.40.0

# Mejora la extracción desde HTML renderizado (app/listing_parser.py).
beautifulsoup4>=4.12.0

# Muestra la foto principal del producto en la tabla de resultados.
pillow>=10.0.0

# Versión WEB para servidor/VPS (app/web_app.py): página accesible desde el
# celular + búsqueda automática programada. Solo se usan en el servidor.
flask>=3.0.0
apscheduler>=3.10.0

# Opcional: app gráfica simple "pegar HTML" (paste_app.py).
# La app completa (app/main.py, el .exe) usa customtkinter, no hace falta.
# PyQt6>=6.6.0
